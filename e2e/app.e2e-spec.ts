import { HdhtPage } from './app.po';

describe('hdht App', () => {
  let page: HdhtPage;

  beforeEach(() => {
    page = new HdhtPage();
  });

  it('should expect true to be true', () => {
    expect(true).toBe(true);
  });
});
